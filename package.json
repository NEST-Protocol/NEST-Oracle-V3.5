{
  "scripts": {
    "clean": "rm -rf build",
    "ozc": "oz compile",
    "tc": "truffle compile",
    "test": "oz compile && npx mocha --timeout 10000 --exit --recursive",
    "tctest": "truffle compile && npx mocha --timeout 10000 --exit --recursive"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.0.42"
  },
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/test-environment": "git+https://github.com/OpenZeppelin/openzeppelin-test-environment.git#80179acb79b31456e4ad8427523555d241300447",
    "@openzeppelin/test-helpers": "^0.5.6",
    "chai": "^4.2.0",
    "mocha": "^8.1.1"
  }
}
